# React Embeddable Demo

Drop-in `<ClaudeChat />` component with zero custom code.

## Run

```bash
node examples/react-embeddable/server.js
```

Open http://localhost:4010

## What This Demonstrates

- Simplest integration: one component, full chat UI
- Server + static HTML served together
- Dark theme by default
- Session management built-in
